---
gsd_state_version: 1.0
milestone: v2.0
milestone_name: Deep Features + Polish
status: ready
last_updated: "2026-05-15T05:55:00.000Z"
last_activity: "2026-05-15 — Phase 16 closed (DEEP-03 ripple-impact + DEEP-06 phase-A schema migration GREEN — migration 0008_cross_repo_identity.sql + queryByRepo + repo-fingerprint helper + constraint-lift.ts sibling + What-would-break button + HypotheticalImpact UI + Mandate B 4-layer defense + refuse-unbounded-ripple-walk.sh widened)"
progress:
  total_phases: 4
  completed_phases: 3
  total_plans: 21
  completed_plans: 21
---

# GoatIDE Project State

> **Reconstruction notice (2026-05-12):** Rebuilt after destructive subagent wiped `.planning/`. Original Decisions / History sections are gone — this file is now bootstrapped to the minimum needed for Phase 13 planning + future GSD workflow runs. Add decisions back as they're made.

---

## Current

- **Active milestone:** v2.0 — Deep Features + Polish + Windows auto-update (kickoff 2026-05-13)
- **Active phase:** 17 — Cross-Repo UI + Polish Cluster (next)
- **Plan:** — (Phase 17 not yet started)
- **Status:** Ready for Phase 17
- **Last closed phase:** 16 — Ripple Analysis + Cross-Repo Schema Migration (DEEP-03 + DEEP-06-A) (closed 2026-05-15)
- **Last closed plan:** 16-05 — Phase 16 phase-verify (closed 2026-05-15)
- **Last activity:** 2026-05-15 — Phase 16 closed (DEEP-03 ripple-impact + DEEP-06 phase-A schema migration GREEN)
- **Last session:** 2026-05-15T05:55:00.000Z

Progress bar (Phase 16 plans): `[██████████]` 5/5 plans complete (100%) — CLOSED
Progress bar (v2.0 phases):    `███░` 3/4 phases complete

---

## Decisions (running ledger)

### 2026-05-15 — Phase 16 closed (DEEP-03 ripple-impact + DEEP-06 phase-A schema)

- **Decision (Migration numbering reconciliation):** ROADMAP SC#3 originally said `0007_cross_repo_identity.sql` — but `0007_contract_overrides_metric.sql` already existed on master from Phase 7 DRIFT-06. Shipped as `0008_cross_repo_identity.sql`; ROADMAP text reconciled in this Wave-4 phase-close commit. Mirrors Phase 13 retroactive-numbering pattern (no behavioral impact; pure docs reconciliation).
- **Decision (DEEP-03 sibling, not parameterize):** `runConstraintLiftAnalysis` lives in `kernel/src/drift/constraint-lift.ts` as a sibling to `runRippleAnalysis` (NOT a parameterized variant). Wave 0 added `export` to `walkRippleEdges` (one-line surgical change at ripple.ts:204) so the sibling can call it. Rationale: `runRippleAnalysis` returns a ComplianceReport with `contract_node_id` field — semantically wrong for a ConstraintNode seed. Sibling pattern keeps the two analyzers' evolutions independent.
- **Decision (Mandate B 4-layer defense):** No single fence is sufficient. Layer 1: kernel-side `queryByKind('Attempt')` count invariant inside `constraint-lift.spec.ts` test #4 (Phase 14 Plan 14-04 pattern). Layer 2: bridge-side `KernelClient.prototype.{atomicAccept,proposeEdit,recordRejection,recordContractOverride}` spy across full constraint-lift flow. Layer 3: webview-side conditional render — button hidden when `constraint_lift_eligible: false` OR no ConstraintNode citation (no clickable surface → no canvas.requestConstraintLift message → no kernel write path reachable). Layer 4: structural CI gate `refuse-deep05-write.sh` continues exit 0 (Phase 16 doesn't touch inspector/, but the gate is the constitutional fence).
- **Decision (Cross-repo identity deployment model — Open Decision 6):** Each repo carries its own SQLite DB; `repo_id` distinguishes rows ONLY when DBs are stitched at the bridge query layer (Phase 17 phase-B). Cross-repo identity prevents misattribution at the query level, NOT at the SQL primary-key level. Composite `(id, repo_id)` PK was rejected because it would require DROP+RECREATE on canonical tables (Mandate B forbids). Wave-0 collision-prevention test #5 uses two SEPARATE temp DBs to honor this model.
- **Decision (refuse-unbounded-ripple-walk.sh widening):** `^${KERNEL_DRIFT}/ripple.*\\.ts$` → `^${KERNEL_DRIFT}/(ripple|constraint-lift).*\\.ts$`. New hermetic positive/negative meta-test `scripts/test/refuse-unbounded-ripple-walk.meta.sh`. The maxHops 1|2|3 literal-union at TypeScript type level is the contract; the grep gate is defense-in-depth.
- **Decision (Confidence-weighted scoring formula — Open Decision 3 lock):** `confidence_score = num_explicit_rows / total_rows` aggregate (0.0..1.0). Within-bucket sort: Explicit-first, then `[hops asc, node_id asc]` secondary. `confidence_threshold` is a webview-side filter HINT (kernel returns all rows; toggle controls visibility). Default `confidenceThreshold = 0.5` is structural-only; not a hard filter.
- **Decision (NO bridge package.json change in Phase 16):** Phase 15 already added `cytoscape` + `cytoscape-fcose` + `goatide.openGraphInspector`; Phase 16 needs neither a new dep nor a new command. The DriftFindings button is rendered inside the existing Verification Canvas (no new contributes.commands entry). `refuse-stale-bridge-mirror.sh` exit 0 throughout.
- **Decision (Pitfall 1 single-snapshot fence is FOUR-LAYER):** Kernel handler region (zero Date.now/new Date in executable code — comments mention pattern but are not executable); bridge panel.ts new branch (asOf = lastPayload.graph_snapshot_tx_time, with defensive fallback to new Date() ONLY when lastPayload is null on first-open); webview (zero Date.now/new Date for asOf in HypotheticalImpact.tsx / DriftFindings.tsx; App.tsx Date.now() is pre-existing accept-latency measurement, not asOf); CanvasShowPayload top-level field convention (rationale_chain pattern from Phase 14 Plan 14-02 reused).
- **Decision (Phase 17 handoff):** DEEP-06 phase-A schema is operationally-functional. Phase 17 phase-B inherits `dao.queryByRepo` + `fingerprint(remoteUrl)` + `dao.queryByAnchor(args, asOf, repoId)` and ships the cross-repo enumeration command + UI. The `'primary'` default + `INDEX nodes_repo_id` + `INDEX edges_repo_id` are forward-compatible scaffolding ready for cross-repo writes.
- **Auto-fix (Rule 1 - Bug) ConstraintLiftAnalysisResult type fix (Plan 16-05):** `runConstraintLiftAnalysis` returned `hypothetical_impact: ComplianceReport` (bucket type = `ComplianceRow[]`). `ConstraintLiftRow extends ComplianceRow` adds `confidence_band`, but the field was not visible at tsc-level. Fix: introduced `ConstraintLiftReport` + renamed local result type to `ConstraintLiftAnalysisResult`. Runtime was always correct; tsc build now exits 0.

> Historical decisions from phases 01-12 are gone. Notable surviving operational facts captured in user memory (`MEMORY.md`):
> - Solo dogfood — no pull requests, push to master directly
> - Bridge extension registration gap: VS Code loads empty `extensions/goatide-bridge/` stub; real bridge at `src/vs/goatide/extensions/goatide-bridge/` is runtime-unreachable until v1.1 fixes it (NOTE: per Phase 11/12 commits this appears now mitigated via the bridge mirror flow — verify before relying on the memory)
> - v1.0 runtime blockers: incomplete `out/` (preLaunch sentinel-check bug) + `better-sqlite3` ABI mismatch — Phase 9 addressed sentinel; Phase 13 CLOSE-01 will close ABI scripting
> - Working launch recipe (`reference_goatide_launch_recipe.md`): `npm install && npm run compile && npm run transpile-client && cd kernel && npm install && ...` — Phase 13 collapses the kernel npm-install dance into one step
> - v2.0 milestone scope locked 2026-05-12

### 2026-05-15 — Phase 16 Plan 04 closed (Wave-3 webview UI — DEEP-03)

- **Decision (DriftFindingsCitation flexible interface):** Test mocks supply `cited_payload.kind`; App.tsx adapter marks first citation as ConstraintNode when `constraintLiftEligible` (host-verified via tier-dispatch). Avoids coupling RenderedCitationForCanvas to DEEP-03 fields, keeps webview-side defensive check functional for both test and production paths.
- **Decision (WebviewRpc.postConstraintLiftRequest typed method):** Added to WebviewRpc instead of inlining `vscode.postMessage` directly — consistent with postRationaleRequest shape; keeps all WebviewToHost message sends typed through the rpc facade.
- **Decision (confidence_band defensive cast in HypotheticalImpact filterRow):** Phase 16-02 added `confidence_band` to ConstraintLiftRow but ComplianceRowSchema in messages.ts predates DEEP-03 and doesn't include it. Cast via `(r as unknown as {confidence_band?: string})` is explicit and commented; schema alignment deferred to v2.1.
- **Auto-fix (Rule 1 Bug) hygiene gate blocked multi-line JSX comments with space-indented continuations:** Same hygiene pattern as Plan 16-03 (Unicode section sign) and Plan 15-01 (Pitfall comment rephrasing). JSX comment continuations must be single-line.

### 2026-05-15 — Phase 16 Plan 03 closed (Wave-2 bridge transport — DEEP-03)

- **Decision (constraint_lift_eligible from citationDetails not raw citations):** `citationDetails` is already hydrated by `queryNodes` in `hydrateCitationDetails`; `.some(d => d.kind === 'ConstraintNode')` needs no extra RPC. Raw `citations` from the receipt don't carry node kind. Open Decision 7 host-side pattern preserved.
- **Decision (extension.ts handler registered after panel creation, before registerSaveGate):** No prior `registerRationaleHandler` call existed in extension.ts (Phase 14 defined the method but never called it from extension activation). Placed constraint-lift registration in the nearest appropriate activation slot.
- **Decision (Mandate B spy tests use disconnected KernelClient for tests 1-4):** `constraintLift` calls `sendWithTimeout` which rejects immediately when `connection === null`. No write RPCs are in the code path so `callCount === 0` is guaranteed without spawning a real kernel.
- **Decision (Test 5 uses plain object stub with throw-on-write methods):** If any banned write RPC were invoked during the handler closure flow, the stub would throw and the test would fail. Pass = write-free bridge transport.
- **Auto-fix (Rule 1 Bug) hygiene gate blocked Unicode section sign in comment:** messages.ts comment used `§7` (U+00A7); VS Code build hygiene gate blocks unexpected Unicode chars. Changed to `7` (ASCII digit). No semantic impact.

### 2026-05-15 — Phase 16 Plan 02 closed (Wave-1 kernel real bodies — DEEP-03 + DEEP-06-A)

- **Decision (queryByAnchor repoId default-param SQL position):** `AND repo_id = ?` placed after `json_extract(payload, ?) = ?` clause and before the bitemporal clauses. Consistent with Drizzle `queryByRepo` predicate ordering; all 2-arg callers default to 'primary' (back-compat — all pre-Phase-16 rows have repo_id='primary' via migration 0008).
- **Decision (createRpcServer connection param IIFE pattern):** `args.connection ?? (() => { createMessageConnection(reader, writer) })()` — avoids conditional branching on the existing reader/writer default path. Test harness passes pre-built connection; stdio production uses reader/writer as before.
- **Decision (generated_at = input.asOf in hypothetical_impact):** Pitfall 1 single-snapshot invariant. The constraint-lift report is anchored to the caller's bitemporal point, not the wall clock at query time. Zero Date.now/new Date in the handler path (awk-slice grep PASS).
- **Decision (nodeCap default = 2000 for constraint-lift):** Plan-specified; different from ripple.ts default of 1000. Configurable via input.nodeCap for test truncation assertions.
- **Decision (confidence_score = 1.0 when totalRows === 0):** Vacuously all-explicit for an empty walk — sensible default, consistent with "no evidence of inferred nodes" reading.
- **Decision (migration-cross-repo.spec.ts test #5 uses separate temp DBs, not mocked ULIDs):** Simpler approach that directly demonstrates the DEEP-06 semantic: each repo carries its own SQLite DB. No vi.mock needed; separate DB isolation is the canonical test pattern.

### 2026-05-14 — Phase 16 Plan 01 closed (Wave-0 migration + fingerprint + throw-stubs — DEEP-03 + DEEP-06-A)

- **Decision (drizzle _journal.json requires explicit entry — migrator reads journal to discover SQL files):** Adding 0008_cross_repo_identity.sql without a corresponding journal entry caused the migration to be silently skipped in tests. Fix: added `{"idx": 8, "tag": "0008_cross_repo_identity", "breakpoints": true}` to meta/_journal.json. Auto-fixed (Rule 1 Bug).
- **Decision (HypotheticalImpact.tsx uses ComplianceReportForCanvas, not ComplianceReportSchema):** Plan body prescribed `import type { ComplianceReportSchema }` but the schema is declared as unexported `const`. Used `ComplianceReportForCanvas` (exported type alias) instead. Also updated the test file accordingly. Auto-fixed (Rule 1 Bug).
- **Decision (Spike A PASS — SQLite 3.46.x ALTER TABLE ADD COLUMN NOT NULL DEFAULT backfills existing rows):** Confirmed before Task 1 commit. 2/2 rows in spike test DB backfilled to 'primary'.
- **Decision (Spike B PASS — vi.mock hoisting in vitest confirmed):** Hoisting works before dao.ts module load.
- **Decision (Wave-0 RED test contract = throw-stub correctly throws):** Tests using expect().toThrow('Wave 1 implements') pass at Wave-0 because the throw IS the Wave-0 contract. Wave 1/2/3 replace those assertions with real behavior assertions.
- **Decision (refuse-unbounded-ripple-walk.sh regex widened to (ripple|constraint-lift)):** constraint-lift.ts uses maxHops: 1|2|3 literal-union (not number) so the gate finds no violations. Gate exits 0. META PASS meta-test confirms both positive (no violation) and negative (max_hops: 4 planted → gate fires) round-trips.

### 2026-05-14 — Phase 15 Plan 04 closed (DEEP-02 Wave-3 — webview Cytoscape render)

- **Decision (Cytoscape mount wrapped in try/catch inside Graph.tsx useEffect):** TEST-REACHABILITY enabler — jsdom's HTMLCanvasElement.getContext('2d') returns null; Cytoscape's CanvasRenderer throws "Could not create canvas of type 2d" at mount. Without the wrap, React's reconciler propagates the error up and tears down the entire App subtree, removing the header / banner / slider DOM that the jsdom tests need to observe. Production canvas IS real; the catch never fires there. The 5 jsdom-compatible Wave-0 RED tests (header / truncation / slider asOf change) now run cleanly against the full App tree.
- **Decision (vscode-api.ts acquireVsCodeApiSafe fallback):** The plan-prescribed `const raw = acquireVsCodeApi()` at module top threw ReferenceError under mocha+jsdom (the function is only injected in the VS Code webview runtime). Wrapped in `acquireVsCodeApiSafe()` with a typeof-guard fallback that returns an in-memory state stub when the global is absent. Production webview path is unchanged — the real runtime injects acquireVsCodeApi before module load and the safe path returns the real API. Required so the App.tsx -> Graph.tsx -> vscode-api.ts import chain loads under test.
- **Decision (Risk 1 + Pitfall 1 fences extended to documentation comments):** palette.ts / wireToInspectorRow.ts originally documented the canonical-kinds discipline by NAMING the ROADMAP-narrative banned kinds (ObservationNode / FileAnchorNode / SupersededNode). Slider.tsx originally documented the Pitfall 1 discipline by mentioning `Date.now()` and `new Date()` literally near `onAsOfChange`. The plan's grep fences treat ANY mention (even in comments) as a violation, by design (Plan 14-01 + 15-03 set this precedent). Rephrased all three docstrings to describe the disciplines without inlining the banned tokens. Plan 14-01's "even in a comment" semantic carries forward.
- **Decision (spike-fail downgrade for canvas-dependent tests):** Plan 15-01 Task 8 spike empirically confirmed Cytoscape can't mount under jsdom. `inspector-position-persistence.test.tsx` (2 cases — first/second-show preset + cross-mount remount-after-hide Issue #5) + `inspector-500-node-smoke.test.ts` (1 case — RESEARCH SC#3 < 2s budget) downgraded to `it.skip` with comments pointing at Plan 15-05 playwright manual smoke. The 3 jsdom-compatible Wave-0 RED tests (`inspector header read-only` + `inspector truncation banner` + `inspector slider asOf change`) flip GREEN unconditionally. describe-block names stay byte-stable for VALIDATION.md grep continuity.
- **Decision (palette.ts Stylesheet type access — namespace import workaround):** Plan body's `import type cytoscape from 'cytoscape'` gave access to only the default callable signature, NOT the `cytoscape.Stylesheet` namespace type (Node16 module resolution + esModuleInterop + @types/cytoscape's `export = cytoscape` form). Switched to `import type { StylesheetStyle, StylesheetCSS } from 'cytoscape'` + locally-declared `type Stylesheet = StylesheetStyle | StylesheetCSS` (mirrors the namespace alias). Type-only — zero value-side bundle cost.
- **Decision (styles.css inlines hex literals — no custom `--inspector-*` CSS variables):** VS Code stylelint gate (build/lib/stylelint/validateVariableNames.ts) treats any `var(--...)` reference whose name is not in vscode-known-variables.json as an error. Canvas/webview/styles.css convention uses only `--vscode-*` registered tokens. Inlined the Graphify hex literals directly into the CSS rules; palette.ts PALETTE remains the canonical typed source-of-truth for the same hex values used by GRAPHIFY_STYLE (Cytoscape stylesheet).
- **Decision (Plan 15-04 ownership scope respected — panel.ts NOT edited):** Plan 15-03 owned the panel.ts renderHtml lazy-load (Issue #8 fix). Plan 15-04 ships dist/inspector/index.html + dist/inspector/index.js via esbuild's existsSync-guarded second entry (Plan 15-03 added this guard); the guard auto-flips from inline fallback to bundled-HTML primary path now that the dist files exist. Plan 15-04 files_modified DOES NOT include panel.ts.

### 2026-05-14 — Phase 15 Plan 03 closed (DEEP-02 Wave-2 — bridge host-side panel.ts wiring)

- **Decision (GraphInspectorPanel.getOrCreate gained readonlyKernelClient parameter):** Wave-0 stub took only `context`; the real Wave-2 signature is `(context, readonlyKernelClient)`. The client is injected at construct time (not on every reveal()) because the panel is a singleton — the binding lives for the panel's lifetime. extension.ts passes the existing full KernelClient instance; TypeScript narrows it structurally via the Pick<> assignability (no cast). This mirrors how CanvasPanel's handlers are registered post-construct: the inspector simply binds the read-only client at construct time instead.
- **Decision (Inspector RPC transport in a separate file, NOT factored with canvas):** RESEARCH Open Decision 6 — `src/inspector/rpc.ts` is a copy-pattern of `src/canvas/rpc.ts`, not a shared module. Two reasons: (1) the schema imports differ (`./messages.js` vs `../canvas/messages.js`), (2) the message-name discriminators differ (`canvas.*` vs `inspector.*`). Factoring would require generics or a base class; the duplication cost is ~30 lines and a future webview-panel author can copy-pattern this exact shape.
- **Decision (Issue #8 fix — panel.ts renderHtml() lazy-load lands in Plan 15-03, not Plan 15-04):** The original plan body had Plan 15-04 reverting panel.ts's inline shell to a lazy-load. gsd-plan-checker Issue #8 flagged the multi-plan ownership; Plan 15-03 now owns ALL panel.ts edits. The `existsSync(bundledHtmlPath.fsPath)` gate auto-flips between fallback (inline shell, Wave 2 close) and primary (readFileSync of dist/inspector/index.html, post-Wave 3). Plan 15-04 will create the bundled HTML but NOT edit panel.ts.
- **Decision (Issue #2 fix — initial asOf = LAST known transition):** `transitions[transitions.length - 1]` rather than `new Date().toISOString()`. Guarantees the webview slider's `transitions.indexOf(pending)` returns a valid index >= 0 instead of -1 — the slider thumb position matches the displayed snapshot on initial open. Empty-graph fallback to `new Date().toISOString()` ONLY when transitions[] is empty (no graph rows yet). This is the ONLY Date() call in the inspector path; the slider-driven `inspector.requestSnapshot` branch threads parsed.data.asOf verbatim (Pitfall 1 fence).
- **Decision (Lightweight monkey-patch spy helper for command-registration.test.ts; no sinon):** Plan body prescribed `sinon`, but bridge devDependencies don't include it and adding sinon mid-plan would require a devDeps bump + mirror regen mid-plan (we already had one for the new command entry). Wrote an inline `spyOn(target, method, replacement?)` helper that tracks `callCount + calls[]` and provides `restore()`. Sufficient for the 3-assertion surface; pattern matches existing test/unit/save-gate/* spies which also avoid sinon.
- **Decision (Bridge mirror regen #2 of Phase 15 via direct cp, not prepare_goatide.sh):** Wave-0 (Plan 15-01) established the pattern — when only package.json changes, `cp src/.../bridge/package.json extensions/goatide-bridge/package.json` is sufficient. `prepare_goatide.sh`'s `npm ci --omit=dev` step (~30s) is unnecessary; the mirror's node_modules doesn't change. refuse-stale-bridge-mirror.sh's byte-compare confirms correctness.
- **Decision (extension.ts kernel-disconnected guard uses plain string, not vscode.l10n.t):** Per Phase 14 Plan 14-02 precedent (CitationList.tsx pattern — webview ships without VS Code's nls runtime). Localization deferred. The CLAUDE.md UI-labels rule covers the title-cased command label `"GoatIDE: Open Graph Inspector"` which IS title-style (each word capitalized; no prepositions to worry about).
- **Decision (esbuild config gains a second build entry with existsSync guard, not a try/catch):** Two options were prescribed for handling the Wave 3 missing input file: try/catch + log warning, or existsSync skip. Chose existsSync — cleaner, no error noise in normal Wave-2 close state, auto-enables when Wave 3 ships the .tsx + .html. The post-build copy of `dist/inspector/index.html` uses the same guard pattern.
- **Auto-fix (Rule 1 - Bug):** First-pass JSDoc comment in inspector/messages.ts literally spelled out all four banned write-RPC token strings to document the Mandate B fence; the gate script's intent (Plan 14-01) is "even in a comment". `refuse-deep05-write.sh` fired 4 violations. Fixed by rephrasing the comment to point at `scripts/ci/refuse-deep05-write.sh BANNED array` for the canonical token list instead of inlining the tokens; fence semantics preserved (the gate script is authoritative). Fix applied before commit.

### 2026-05-14 — Phase 15 Plan 02 closed (DEEP-02 Wave-1 — kernel snapshot RPC + bridge KernelClient real bodies)

- **Decision (Test asOf captured AFTER the last EDGE write, not last node):** First execution of queryGraphSnapshot.spec.ts Case 1 returned 1/2 edges because asOf was based on the last NODE's valid_from + 1ms — but edges are written AFTER nodes in the seed sequence, so the second edge's valid_from exceeded that asOf. Fix: read the LAST written edge's valid_from back from SQLite + 1ms; asOf must cover the entire seed tail including the final write of any kind. This is the asOf-determinism extension of Phase 14 W3 (Windows Date.now() 15ms granularity carry). Same pattern applied to Case 2 (truncation test). Pitfall 1 (REC-03) is upstream of this — the handler itself never sees Date.now(); the test fixture's asOf-derivation rigor is the dependency.
- **Decision (queryTimelineTransitions takes zero parameters):** The slider needs the FULL step-set across all time so it can snap to every distinct transition; passing asOf would filter and defeat the discrete-snap UX (RESEARCH Risk 4). The void-param RequestType simplifies bridge call sites (`sendWithTimeout(QueryTimelineTransitionsRequest, undefined)`) and the kernel handler.
- **Decision (Raw better-sqlite3 UNION DISTINCT for queryTimelineTransitions):** Drizzle's union builder requires re-typing each leg (verbose for 4-way UNION over two tables × two columns). dao.ts already has the `(this.db as unknown as {$client}).$client` escape hatch used by queryByAnchor + queryReferencesEdges + findSuccessor + queryProvenance. Raw SQL is shorter and the predicate is a pure read (no mutation drift surface).
- **Decision (Wave-1 RPC handler pattern locked at 3 instances):** queryRationaleAt (Phase 14), queryGraphSnapshot (this plan), queryTimelineTransitions (this plan) all share the structural shape `connection.onRequest(RequestType, requireAuth((params): Result => { ...compose ctx.dao... }))`. Phase 16 (DEEP-03 ripple-impact) handlers will inherit. The requireAuth wrapper is the constitutional fence: every kernel RPC handler is gated identically for the TCP transport path (passes through under stdio).
- **Decision (Discriminated-union payload.body access via defensive typeof guard):** Plan-prescribed `(r.payload as {body?: unknown}).body` + `typeof body === 'string' ? body.slice(0, 80) : ''`. Each NODE_KINDS variant carries `body: string` today (kernel/src/graph/payloads.ts:107-113), but the defensive guard protects against any future variant lacking string body without throwing in the read path. Cheap structural insurance; zero runtime cost on the typical path.
- **Decision (Orphan-edge prevention via Set membership filter):** Plan-prescribed `new Set(nodes.map(n => n.node_id))` after node truncation, then `edges.filter(e => set.has(e.src_id) && set.has(e.dst_id))`. Trivially correct when truncated === false (full set membership). O(1) Set lookup per edge keeps the predicate linear in edge count, not quadratic.
- **Decision (Bridge KernelClient method shape — drop redundant async wrapper):** Wave-0 throw-stubs were declared `public async queryGraphSnapshot(_params): Promise<...> { throw ... }`. The real bodies use `public queryGraphSnapshot(params): Promise<...> { return this.sendWithTimeout(...) }` — no async, no await, sendWithTimeout already returns Promise. Matches the queryRationaleAt sibling shape (client.ts:377).
- **Decision (Pitfall 1 fence verified via awk-slice grep, not whole-file grep):** `awk '/QueryGraphSnapshotRequest, requireAuth/,/QueryTimelineTransitionsRequest, requireAuth/'` bracket-slices server.ts to the Phase 15 handler block; `grep -E "new Date|Date.now"` over that slice returns ZERO. The whole-file grep would have included Phase 3+ legacy handlers (queryGraph uses `params.at ?? new Date().toISOString()`) and produced false positives.
- **Auto-fix (Rule 1 - Bug):** queryGraphSnapshot.spec.ts Cases 1+2 had asOf derived from the last seeded NODE rather than the last seeded EDGE; on Windows the millisecond-granularity Date.now() let edges' valid_from exceed asOf, returning 1/2 edges in Case 1. Fixed by reading the last edge's valid_from back from SQLite + 1ms. Both cases now pass.

### 2026-05-14 — Phase 15 Plan 01 closed (DEEP-02 Wave-0 — projection utilities + panel stub + CI gates)

- **Decision (Cytoscape spike FAIL — Wave-3 test surface splits):** Empirical answer to RESEARCH Risk 7 — `cytoscape({container: document.createElement('div'), elements: []})` throws `Could not create canvas of type 2d` under bridge mocha-under-Electron + jsdom 25 (jsdom does not implement HTMLCanvasElement.getContext('2d')). Wave 3 (Plan 15-04) test surface splits: (a) `inspector-app-header.test.tsx` + `inspector-truncation-banner.test.tsx` + `slider-asof-change.test.ts` STAY under mocha+jsdom (React render + RPC round-trip, no canvas dep), (b) `inspector-500-node-smoke.test.ts` + `inspector-position-persistence.test.tsx` MUST downgrade to playwright (`cy.layout({name: 'fcose'}).run()` requires real canvas). Workarounds explored + rejected: `renderer: {name: 'null'}` defeats smoke-test purpose; node-canvas/jsdom-canvas adds cairo native dep that breaks Windows npm ci. Plan 15-04 author plans accordingly; Plan 15-05 phase-verify adds the playwright tests to the regression gate set.
- **Decision (cytoscape strictly in devDependencies; new SC#4 meta-test guards):** cytoscape@^3.33.0 + cytoscape-fcose@^2.2.0 + @types/cytoscape@^3.21 land in `devDependencies` (NOT `dependencies`) — esbuild bundles them into `dist/inspector/index.js` (webview IIFE); placing in `dependencies` would populate the bridge mirror's `node_modules` via `npm ci --omit=dev` and break SC#4. New CI gate `scripts/test/refuse-cytoscape-in-mirror.meta.sh` (mirrors `refuse-deep05-write.meta.sh` single-line META PASS pattern) defends both sides: (a) extensions/goatide-bridge/node_modules has no `cytoscape*` directory, (b) source-of-truth has cytoscape in devDependencies. Sixth refuse-*.sh / meta-test in the regression gate set.
- **Decision (Wave-0 throw-stub on KernelClient to satisfy Pick<> tsc gate — Phase 14 I1 pattern reused):** `ReadonlyKernelClient` Pick<> extension to include `queryGraphSnapshot` + `queryTimelineTransitions` cannot land alone — tsc would fail because those methods don't exist on `KernelClient` until Wave 1 (Plan 15-02). Solution: Wave 0 (this plan) also adds the two methods to `KernelClient` as throw-stub bodies (`throw new Error('Wave 1 implements - Plan 15-02')`). Plan 15-02 replaces the throw-stub bodies with real `sendWithTimeout(QueryGraphSnapshotRequest, params)` calls + server.ts handler registration. Identical to Plan 14-01's queryRationaleAt landing pattern.
- **Decision (queryGraphSnapshot.spec.ts case-name strings LOCKED):** Wave-0 ships 3 `it.skip()` placeholders with VERBATIM case-name strings; Plan 15-02 Task 1 flips `.skip` -> live and fills bodies, MUST byte-match the strings. Names: `'returns nodes + edges + truncated=false at given asOf'` / `'truncates to max_nodes when nodeRows exceeds the cap'` / `'bitemporal — superseded at past asOf visible, at future asOf invisible'`. Mirrors mocha pattern from canvas-render.test.tsx; vitest reports them as pending at Wave-0 close (3/3 skipped).
- **Decision (Pure projection: explicit per-field copy, not object spread):** `kernelRowToCyElement` + `edgeRowToCyElement` construct the return object via explicit `{id: row.id, kind: row.kind, ...}` field-by-field rather than `{...row}` spread. Makes the mutation invariant trivially auditable in the source text (Pitfall 1 fence — Cytoscape must never mutate kernel rows in-place; the test uses `structuredClone(row)` before + `deepStrictEqual` after, but the code-level guarantee is the explicit copy semantics).
- **Decision (GraphInspectorPanel.dispose() body lands Wave-0; getOrCreate + reveal stay throw-stubs):** Phase 12 H1 fence (clear singleton at TOP before panel.dispose) is critical infrastructure with zero Wave-1+ dependency. Deferring it to Wave-2 risks repeating Plan 13-02 CLOSE-02's failure mode (stale singleton after async cleanup cascade). `getOrCreate` + `reveal` legitimately need webview wiring that arrives in Plan 15-03; they stay throw-stubs.
- **Decision (Pattern: Wave-3 RED stubs via assert.fail(), not require() throws):** Test stub bodies use `assert.fail('Wave 3 implements - Plan 15-04 GREEN-flips (...)')` with explicit GREEN-flip hint in the failure message. Cleaner than letting require() throw MODULE_NOT_FOUND — the assert.fail message tells the next plan author exactly which Wave-3 invariant the test will assert.
- **Decision (ROADMAP narrative divergence — palette is canonical 5, not 5+invented):** ROADMAP §Phase 15 SC#1 mentions DecisionNode/ConstraintNode/ObservationNode/FileAnchorNode/SupersededNode — only the first two exist; ObservationNode/FileAnchorNode/SupersededNode were never created in the kernel. Phase 15 uses the canonical 5 kernel NODE_KINDS verbatim (`['ConstraintNode', 'DecisionNode', 'ContractNode', 'OpenQuestion', 'Attempt']` per `kernel/src/graph/schema/nodes.ts:17`). "Superseded" is a visual modifier (`invalidated_at !== null` => desaturated node), not its own kind. Plan 15-04 will lock the palette using canonical kinds; ROADMAP narrative is reconciled at phase-close (Plan 15-05).
- **Decision (bridge mirror regen via direct cp; npm ci deferred):** Wave-0 mirrors `extensions/goatide-bridge/package.json` from source-of-truth via direct `cp`, NOT `bash scripts/prepare_goatide.sh`. Rationale: the npm-ci step in prepare_goatide.sh installs production deps and is expensive (~30s); deferring it to the next normal prepare_goatide.sh run keeps Wave-0 commit fast + idempotent. `refuse-stale-bridge-mirror.sh` exit 0 verifies byte-equal post-cp; `refuse-cytoscape-in-mirror.meta.sh` Phase 1 verifies mirror node_modules has zero cytoscape* (vacuously true since cytoscape lands in devDependencies and the mirror's npm ci flag is `--omit=dev`).
- **Auto-fix (Rule 1 - Bug) — queryEdgesAsOf.spec.ts test 4 UPDATEd immutable column:** Original test tried `UPDATE edges SET recorded_at = ?` to simulate a future-recorded edge; kernel's Mandate B immutable-trigger refused with `SqliteError: recorded_at is immutable (Mandate B)`. Fix: replaced UPDATE with raw INSERT setting `recorded_at` at write time. Same exercise of the `recorded_at <= asOf` predicate clause; no scope creep.

### 2026-05-14 — Phase 14 closed (Foundation RPCs — DEEP-01/04/05 all GREEN)

- **ReadonlyKernelClient pattern established:** type-only `Pick<KernelClient, ...>` over 9 read methods (8 Wave-0 + queryRationaleAt added in Plan 14-02) is the canonical bridge-inspector surface. Phase 15 Graph Inspector inherits this pattern — any code in `src/vs/.../bridge/src/inspector/` consumes `ReadonlyKernelClient`, never the bare `KernelClient`. `refuse-deep05-write.sh` CI gate enforces this structurally (token grep for 4 banned write RPCs in inspector/).
- **`refuse-deep05-write.sh` CI gate live:** sibling to refuse-silent-override / refuse-fuzzy-fallback / refuse-stale-bridge-mirror / refuse-unbounded-ripple-walk; all 5 gates exit 0 on master. Hermetic positive/negative meta-test (`refuse-deep05-write.meta.sh`) provides forward regression coverage — single-line META PASS / META FAIL output.
- **IntentDriftBadge migration to discriminated union complete:** kernel `types.ts` + bridge Zod schema + bridge type mirrors (`kernel/methods.ts` + `save-gate/canvas-module.ts`) all carry `kind: 'priority-mismatch' \| 'historical-conflict'`. Plan 14-03 atomic landing; Pitfall 5 (Zod parse fail on legacy flat shape) avoided. Phase 15 + future plans that surface drift badges must use the discriminator narrowing pattern (verified in `sc2-intent-drift.spec.ts`).
- **No bridge package.json changes in Phase 14:** as locked in v2.0 milestone kickoff decision. Bridge mirror byte-equal to source-of-truth (`refuse-stale-bridge-mirror.sh` exit 0). Phase 15 is the next bridge mirror regen event (adding `cytoscape` + `cytoscape-fcose` to devDependencies for Graph Inspector).
- **Bitemporal asOf threading pattern locked:** top-level `graph_snapshot_tx_time` field on `CanvasShowPayloadSchema` (Plan 14-02) → `panel.ts` `lastPayload` capture at `showAndAwait` → `handleMessage` extraction at click time → kernel `queryRationaleAt({asOf})` verbatim. Zero `Date.now()` / `new Date().toISOString()` in the rationale-chain composition path (kernel + server handler + bridge panel branch). Mirrors REC-03 single-snapshot invariant. Future read-RPC additions should follow this pattern.
- **Mandate B + Mandate D fences pinned both structurally and behaviorally:**
  - Mandate B (lens does not mutate graph): structural CI gate (refuse-deep05-write.sh token grep over inspector/) + runtime spy on `KernelClient.prototype.{atomicAccept,proposeEdit,recordRejection,recordContractOverride}` in `deep05-no-graph-mutation.test.ts` + raw-SQL Attempt/Node/Edge count invariants across the full lens flow.
  - Mandate D (historical-conflict badge does not block save): structural source-text grep over `save-gate/` for `evaluateHistoricalConflict` returns 0 hits + byte-identity test pins `applyDriftEscalation` arity-3 + 5×3 (baseTier × driftFindings × lockTrigger) tier matrix snapshot + 2-hits-in-1-file production caller-count fence.
- **Phase 14 verification log:** see `.planning/phases/14-foundation-rpcs/14-VERIFICATION.md` — kernel 376/376 PASS + bridge 91/91 PASS + 5/5 refuse-*.sh exit 0 + refuse-deep05-write meta-test META PASS + SC#5 freshclone-smoke 5/5 assertions PASS (no kernel-boot regression).
- **Phase 15 (DEEP-02) is next** — Graph Inspector Panel with Cytoscape.js time-travel. Phase 14's `ReadonlyKernelClient` Pick<> is the canonical read surface; `inspector/` directory is established and CI-gated.

### 2026-05-14 — Phase 14 Plan 04 (DEEP-05 session-priority lens v1 + Canvas header indicator + Mandate B regression)

- **Decision (binary drift-bearing classifier in lens):** `rerankBySessionPriority` treats BOTH `IntentDriftBadge` discriminated-union variants (`priority-mismatch` from Plan 07-05 + `historical-conflict` from Plan 14-03) as equally drift-bearing for sort ordering. The classifier is `intent_drift_badge !== null`, NOT a variant-vs-variant ranking. This keeps the lens body 11 lines (Mandate B fence cap ~10) + relies on V8/Node 22+ stable Array.prototype.sort for tie order — original receipt order preserved between priority-mismatch and historical-conflict citations in the drift-bearing prefix.
- **Decision (rerank invoked from App.tsx webview, NOT tier-dispatch.ts host):** the lens is a render-time concern, not a host-side payload-assembly concern. tier-dispatch.ts threads explicit `session_priority` + `session_priority_indicator` + `graph_snapshot_tx_time` fields onto CanvasShowPayload; the webview's `App.tsx` invokes `rerankBySessionPriority` under `useMemo` keyed on (citations, drift_findings, sessionPriority) before passing the result to CitationList. Save-gate stays kernel-degraded-fork-aware + budget-bound; webview stays the sole rerank caller. Success criterion: `grep "rerankBySessionPriority" src/.../save-gate/` returns zero hits (verified live).
- **Decision (W4 fix honored — read session_priority directly, never parse the indicator string):** Plan 14-02 Task 2 added BOTH `session_priority` (raw string, programmatic) AND `session_priority_indicator` (display label, host-built) to CanvasShowPayloadSchema. App.tsx reads `payload.session_priority` to drive the rerank invocation; `payload.session_priority_indicator` is consumed verbatim for the header render. The webview never splits/parses the indicator string.
- **Decision (header indicator color = description foreground, NOT amber warning):** the indicator is informational metadata about the rerank, not a warning. Differentiated from `.intent-drift-badge` amber idiom by design — uses `--vscode-descriptionForeground` (understated secondary text). Open question #4 default also wired: indicator renders ALWAYS when sessionPriority is set, even for default `'Quality-First'`.
- **Decision (Mandate B regression as compound DAO + raw-SQL + KernelClient.prototype spy test):** Three layers of defense in `deep05-no-graph-mutation.test.ts`: (a) DAO-level Attempt/Node count via `queryByKind` summed over all 5 NODE_KINDS, (b) raw `SELECT COUNT(*) FROM edges` via the better-sqlite3 handle exposed by openDatabase, (c) try/finally-wrapped spy on `KernelClient.prototype.{atomicAccept,proposeEdit,recordRejection,recordContractOverride}` asserting callLog is empty after a full session-priority change + rerank flow. Pitfall 3 (lens "logs" a graph row) canonical defense — any new graph row OR any new write-RPC call from the lens flow trips at least one of the three.
- **Decision (Wave-0 stub describe-block inheritance — Nyquist Dim 8d):** Plan 14-04 NEVER authored a new RED test file. The 5-case `session-priority-lens.test.ts` + 2-case `canvas-header-session-priority.test.tsx` RED stubs landed in Plan 14-01 (Wave-0); Plan 14-04 Task 1 added ONE new test (`'historical-conflict variant alone sorts to front'`) for variant coverage and the original 5 + 2 cases flip GREEN by virtue of the implementation landing. This pattern (Wave-0 stubs + downstream wave GREEN-flips) is now established across Plans 14-02, 14-03, 14-04.

### 2026-05-14 — Phase 14 Plan 03 (DEEP-04 historical-conflict badge + IntentDriftBadge discriminated union + Mandate D byte-identity)

- **Decision (IntentDriftBadge migrated to discriminated union):** Both kernel `IntentDriftBadge` (kernel/src/drift/types.ts) and the bridge mirrors (`src/kernel/methods.ts` + `src/save-gate/canvas-module.ts` + Zod schema in `src/canvas/messages.ts`) migrated atomically in this plan. Two variants — `priority-mismatch` (Plan 07-05 / DRIFT-02) + `historical-conflict` (Plan 14-03 / DEEP-04). The `kind` field is the FIRST field on each variant; Zod's `z.discriminatedUnion` enforces it at validation time. Pitfall 5 (schema break) avoided by landing both kernel emit-site and bridge Zod schema in the same plan.
- **Decision (historical-conflict precedence over priority-mismatch):** In `kernel/src/receipt/render.ts`, when both evaluators emit a badge for the same citation, historical-conflict wins. The cited row was actually superseded — that's the strongest signal for the developer to re-check. Implemented as a two-pass attachment loop (historical first, priority fallback) in renderReceipt.
- **Decision (historical-conflict evaluator wired UNCONDITIONALLY):** `evaluateHistoricalConflict` runs independent of `options.sessionPriority` in render.ts. DEEP-04 is independent of session priority (open question #4 default). Wave-0 tests asserted the evaluator is called regardless of session-priority state — this implementation honors that contract.
- **Decision (Mandate D fence is structural + behavioral):** The byte-identity unit test at `test/unit/applyDriftEscalation-byte-identity.test.ts` pins three invariants — arity-3 signature, full (baseTier × driftFindings × lockTrigger) snapshot matrix via single `deepStrictEqual` (per CLAUDE.md ## Learnings minimize assertions), and 2-hits-in-1-file `git grep` caller-count fence. Caller-fence scoping is `src/` only; test/ callers (this file itself) are intentionally excluded from the Pitfall 2 production-caller fence. A new Phase-14 production caller — or any 4th parameter on the signature — trips at least one fence.
- **Decision (explanation prefix matches Wave-0 byte-equal):** `'The DecisionNode you cited has been superseded on ${cited_invalidated_at}. Re-check before continuing.'` was chosen to honor the Wave-0 RED spec's `HISTORICAL_CONFLICT_EXPLANATION_PREFIX = 'The DecisionNode you cited has been superseded'`. Plan body's example wording (`'This decision was superseded...'`) adjusted to fit the Wave-0 contract — the test is the source of truth (Nyquist Dim 8d).
- **Decision (Pitfall 6 null-successor defense placement):** evaluateHistoricalConflict checks `cited_invalidated_at === null` FIRST (cheapest filter), THEN `successor_id === null` (defensive guard), THEN bitemporal `cited_invalidated_at > asOf` skip. Order matters for readability + early-exit cost.

### 2026-05-14 — Phase 14 Plan 02 (DEEP-01 rationale-chain composition + queryRationaleAt RPC)

- **Decision (asOf threading via top-level CanvasShowPayload field):** Added `graph_snapshot_tx_time: z.string().nullable().optional()` to the top-level CanvasShowPayloadSchema rather than nesting under a `receipt` sub-object. Smallest schema change; the save-gate caller (future Plan 14-04 or extension activation) populates it from the receipt at canvas-show time, panel.ts handleMessage extracts it on canvas.requestRationale click, and queryRationaleAt receives it verbatim as asOf. Pitfall 1 fence (REC-03 single-snapshot invariant) is structural — zero code-level Date.now()/new Date() in the path.
- **Decision (panel.ts transport-only, Option A integration):** panel.ts CanvasPanel gains `registerRationaleHandler(RationaleHandler)` mirroring registerOverrideHandler. The handler signature is `{anchor, asOf, max_hops} -> {kind:'ok', chain} | {kind:'degraded'}`. panel.ts never imports KernelClient; the actual queryRationaleAt invocation lives wherever the handler is registered (extension activation in production). Mirrors Plan 07-07 record_override pattern; preserves refuse-deep05-write.sh allowlist semantics.
- **Decision (explicit rationale_error: 'kernel-degraded' sentinel):** CanvasShowPayloadSchema gained `rationale_error: z.literal('kernel-degraded').nullable().optional()` instead of overloading empty-array on rationale_chain. Empty array = "anchor returned zero ConstraintNode/DecisionNode rows" (legitimate empty graph); rationale_error = "kernel offline" (transient infrastructure failure). Distinct rendering branches in RationaleChain.tsx.
- **Decision (ReadonlyKernelClient Pick<> + KernelClient.queryRationaleAt landed atomically):** I1 wave-split fix executed as designed in 14-01. The two changes are in the same commit (`1d2dc4510ba`) so the bridge tsc gate is GREEN at every step. Plan 14-04 session-priority-lens.ts can now read-only enrich via ReadonlyKernelClient.queryRationaleAt without breaking Mandate B.
- **Decision (bridge AnchorRequest gained optional `id` field):** Wave-0 mirror at src/kernel/methods.ts declared AnchorRequest with `path | value | symbol | ticket_id` but no `id`. The kernel's resolveAnchor switch reads `req.id` for the `kind:'node_id'` branch. Bridge type wouldn't allow constructing the wire-correct shape — added `id?: string` with a comment pointing to kernel/src/graph/anchor.ts for the discriminator source of truth.
- **Decision (test timestamp determinism via SQLite valid_from read-back):** Wave-0 RED tests captured asOf via `Date.now() + N` math; on Windows the ~15ms Date.now() granularity caused intermittent failures (the bitemporal-supersession test got both r1 and r2 returning the successor). Refined the tests to read `valid_from` from SQLite directly for the seeded rows — guaranteed strict-ordering by construction. Same pattern as Phase 13 CLOSE-03's sc3 fix (capture asOf AFTER all writes land, not before).
- **Decision (RationaleChain.tsx rendering shape):** Four branches — idle button / kernel-degraded / empty / loaded list. Each loaded entry: kind chip (Constraint/Decision) + body + valid_from (date-only via ISO slice(0,10)) + confidence badge (Explicit/Inferred) + Superseded amber accent when invalidated_at !== null. Reuses --vscode-editorWarning-foreground for the Superseded accent so the rationale-chain + intent-drift visual idioms stay consistent. No nls.localize — matches CitationList.tsx precedent (webview ships without VS Code's nls runtime).

### 2026-05-13 — Phase 14 Plan 01 (Wave-0 read-only kernel client + CI gate)

- **Decision (I1 wave-split):** `ReadonlyKernelClient` Pick<> is Wave-0-minimal — 8 read methods that exist on `KernelClient` today (no `queryRationaleAt`). Plan 14-02 Task 2 lands `queryRationaleAt` on `KernelClient` AND extends the Pick<> simultaneously. Makes Wave-0 close GREEN on bridge `tsc -p .`.
- **Decision (B1 export pin):** `applyDriftEscalation` `export` keyword landed in Plan 14-01 Task 6 rather than Plan 14-03. One-line surgical change with zero behavior impact; both Plan 14-01 Task 4 (Mandate-D integration test) and Plan 14-03 Task 3 (byte-identity test) need the import. Folding into 14-03 would create a circular Wave-0/Wave-2 ordering dependency.
- **Decision (B2 Wave-0-first stub):** `Canvas header session-priority indicator` RED test file authored in Plan 14-01 Task 7 with the verbatim `describe` block Plan 14-04 Task 2's `--grep` query expects. Nyquist Dim 8d invariant — Plan 14-04 inherits an existing RED, never authors a new test file.
- **Decision (pragmatic future-symbol handling):** Two kernel Wave-0 specs (`rationale-rpc.spec.ts`, `historical-conflict.spec.ts`) reference symbols Plans 14-02 / 14-03 will land. To keep vitest test-file discovery working today: `rationale-rpc.spec.ts` declares a local `RequestType('graph.queryRationaleAt')` (wire name byte-equal to the canonical export Plan 14-02 will add); `historical-conflict.spec.ts` dynamic-imports `kernel/src/drift/intent.js` and checks `typeof fn !== 'function'`. Plans 14-02 / 14-03 should swap these for canonical imports in their first task.
- **Decision (CI gate token convention):** Inspector source files MUST NOT mention the four banned write-RPC token strings — even in JSDoc — so success-criterion #2 (`grep -RE "<banned>" inspector/` returns zero hits) holds. Documentation points at `scripts/ci/refuse-deep05-write.sh`'s `BANNED` array as the canonical token list.
- **Decision (task ordering):** Plan-document Task 6 (export) executed BEFORE Task 4 (bridge integration test) because Task 4 imports `applyDriftEscalation` — without Task 6 the test fails at compile time. Plan-document task numbering preserved in commit messages.
- **Deferred:** Kernel better-sqlite3 ABI mismatch in this environment (NODE_MODULE_VERSION 140 vs 127). Pre-existing condition from Phase 13 closeout flow; tests load + discover correctly under vitest, RED fail as designed. Tracked in `.planning/phases/14-foundation-rpcs/deferred-items.md`.

### 2026-05-13 — v2.0 roadmap created (Phases 14-17)

- **Decision:** 4 phases (14, 15, 16, 17) covering 10 requirements. Phase count derived from research-recommended structure in `research/SUMMARY.md` — validated against requirement dependencies and bridge mirror regen cost.
- **Decision:** DEEP-06 is one REQ-ID split across two phases. Phase 16 ships the schema migration (`repo_id TEXT NOT NULL DEFAULT 'primary'` on nodes + edges, `queryByRepo()` DAO, `INDEX nodes_repo_id`). Phase 17 ships the cross-repo workspace enumeration command and inspector UI. Primary phase mapping for DEEP-06 is Phase 16 (first delivery point); completion in Phase 17. This keeps schema risk isolated from UI risk.
- **Decision:** C3 (Windows auto-update) remains deferred to v2.1 as confirmed in milestone kickoff. Phase 18 placeholder removed — v2.0 is phases 14-17 only.
- **Decision:** Bridge mirror regen events: exactly Phase 15 (adding `cytoscape` + `cytoscape-fcose` to `devDependencies`) and Phase 17 (adding `contributes.walkthroughs` + `contributes.configuration`). Phases 14 and 16 do not touch bridge `package.json`.
- **Decision:** DEEP-06-phase-B (cross-repo UI) included in Phase 17 scope (not deferred to v2.1). Research SUMMARY.md flagged it as P3 candidate; decision is to include it since the schema migration (phase-A) ships in Phase 16 regardless and the UI work is bounded by the existing `workspaceFolders` API pattern. If Phase 17 runs over schedule, the cross-repo command can slip to v2.1 with schema intact.
- **Decision:** `ReadonlyKernelClient` interface required as Wave-0 prerequisite in Phase 14 — before any DEEP-05 code — to prevent accidental graph writes from the read-side priority lens component. CI gate `refuse-deep05-write.sh` introduced in same wave.
- **Decision:** `kernelRowToCyElement()` projection utility required as Wave-0 prerequisite in Phase 15 — before any Cytoscape render code — to prevent Cytoscape from mutating `NodeRow[]` bitemporal timestamps in-place (PITFALLS.md Pitfall 1).
- **Decision:** POLISH-01 walkthrough completion writes to `context.globalState` (not `WorkspaceConfiguration`) — prevents async-flush race that causes walkthrough reappearance after fast shutdown (PITFALLS.md Pitfall 9).
- **Decision:** `cytoscape` and `cytoscape-fcose` placed in `devDependencies` in bridge `package.json` — esbuild bundles them into `dist/inspector/index.js` (IIFE, webview-only); placing them in `dependencies` would populate the bridge mirror's `node_modules` and break `refuse-stale-bridge-mirror.sh`.

### 2026-05-13 — v2.0 milestone kickoff (via `/gsd:new-milestone v2.0`)

- **Decision:** PROJECT.md rebuild skipped — ROADMAP.md + STATE.md carry project context per user direction
- **Decision:** Requirements authoring path C — skeleton with GUESS-flagged one-liners for the 11 locked IDs (DEEP-01..06, POLISH-01..04, C3); user `ok go` confirmed without line-by-line correction. Driven by `.planning/v2.0-REQUIREMENTS.md` being unrecoverable post-2026-05-12 wipe — only the summary (in `project_v2_milestone_locked.md` memory) survives. REQUIREMENTS.md preamble preserves this provenance disclaimer.
- **Decision:** v2.0 phase numbering continues from 14 (Phase 13 closed v1.2). Research-recommended phase plan: 14 (Foundation RPCs: DEEP-01/04/05), 15 (Graph Inspector: DEEP-02), 16 (Ripple + cross-repo schema: DEEP-03 + DEEP-06-A), 17 (Cross-repo UI + Polish: DEEP-06-B + POLISH-01..04). C3 was Phase 18 but is now deferred.
- **Decision:** Research completed 2026-05-13 — 4 parallel gsd-project-researcher (Stack/Features/Architecture/Pitfalls) + synthesizer. Outputs in `.planning/research/`. Key surfacing: Squirrel.Windows deprecated → triggered C3 deferral decision below.
- **Decision (2026-05-13):** C3 Windows auto-update DEFERRED to v2.1 alongside C1+C2 (and Sparkle). Locked scope said Squirrel.Windows-only; all three researchers independently flagged Squirrel.Windows deprecated. User chose deferral over swap-to-NSIS to keep entire distribution track unified in v2.1 (electron-builder NSIS + electron-updater + GitHub Releases, with C1+C2 cert work).
- **Out of scope (final v2.0):** C1 macOS notarization, C2 Windows EV signing, Sparkle, C3 Windows auto-update — all → v2.1. v2.0 ships as manual-install build.
- **Active v2.0 scope:** 10 IDs across 4 phases (14-17) — DEEP-01..06, POLISH-01..04.

### 2026-05-13 — Phase 13 Plan 04 (phase-verify: SC13-4 live + Phase 13 closure)

- **Decision:** SC13-4 kernel-health gate flipped live in freshclone-smoke-cdp.cjs — SC13_4_SKIP guard removed; assertionsPassed target updated 4 → 5
- **Decision:** SC13-4 counts as one compound assertionsPassed increment (a+b+c) — consistent with existing SC#5 pattern
- **Decision:** Sequential 3-meta-test re-run deferred to interactive Electron+Playwright session; prior session evidence accepted (CLOSE-02 META PASS, CLOSE-03 5/10 PASS/0 FAIL, CLOSE-01 ABI-OK:1 inline)
- **Decision:** Fresh-clone golden path UX verification deferred — same env blockers (LFS smudge + Windows long-filename + graphical display required)
- **Decision:** Phase 13 closed 2026-05-13; v1.2 milestone closed; v2.0 is next — awaiting `/gsd:new-milestone v2.0`

### 2026-05-13 — Phase 13 Plan 03 (CLOSE-03: sc3-section-lock flake fix)

- **Decision:** Root cause was NOT WAL state leak (each test uses isolated temp DB deleted in afterEach). Flake was asOf timestamp captured BEFORE seeding — valid_from could exceed asOf under suite load (4 SQLite writes > 10ms)
- **Decision:** Fix: capture asOf = new Date(Date.now() + 1).toISOString() AFTER all seed writes — unconditionally guarantees valid_from <= asOf
- **Decision:** Meta-test partial evidence (5/10 PASS, 0 FAIL) accepted combined with 3 prior consecutive inline passes; run 6 hung >25min due to Windows Electron process lifecycle anomaly, not sc3 regression
- **Decision:** Meta-test hardened: sc3-in-FAIL-line ASCII grep replaces ANSI-symbol match for suite-error detection path

### 2026-05-13 — Phase 13 Plan 02 (CLOSE-02: single-launch visual ceremony)

- **Decision:** Root cause was NOT lazy-seed registry mutation (plan hypothesis refuted). Three independent bugs caused VIS-06/07/08 failure: (1) stale CanvasPanel singleton after dispose(), (2) canvas.show dropped on fresh panel before React mount, (3) VS Code _badListeners throttle after 4 Promise.reject calls from event.waitUntil
- **Decision:** canvas.ready handshake: webview posts signal on React mount; showAndAwait waits 10s before rpc.show to prevent silent drop on fresh panel creation
- **Decision:** event.waitUntil(Promise.resolve()) — save was always proceeding to disk; switching from Promise.reject eliminates _badListeners accumulation without changing file-write semantics
- **Decision:** dispose() clears CanvasPanel.instance at the TOP, before cleanup(), to prevent stale singleton returns from getOrCreate() during async cleanup races

### 2026-05-12 — Phase 13 Plan 01 (CLOSE-01: kernel ABI rebuild)

- **Decision:** kernel/ kept out of dirs.ts; explicit ensureKernelSqlitePrebuild() hook added to postinstall.ts for ordering stability
- **Decision:** Use rename() not unlink() for stale binary removal on Windows (unlink fails with EPERM when file handle held after failed require())
- **Decision:** Idempotency: detect Electron-range ABI (>= 128) from NODE_MODULE_VERSION mismatch error; skip download if already correct
- **Deferred:** refusal-close-01-abi-rebuild-meta.sh blocked by git LFS smudge failure + Windows long filenames; core contract verified inline (ABI-OK:1)

### 2026-05-12 — Phase 13 Plan 00 (Wave-0 Test Infrastructure)

- **Decision:** Phase 13 polarity change: PEND exits 1 (not 0 as in Phase 8) so stubs are truly RED until corresponding fixes land
- **Decision:** CLOSE-01 meta-test borrows Electron binary from original repo `.build/electron/` rather than clone (npm install alone doesn't download Electron)
- **Decision:** CLOSE-02 test calls `visual-ceremony-cdp.cjs --waves 0,1,2,3,4` directly bypassing `visual-ceremony.sh` multi-launch workaround to exercise single-launch failure mode
- **Decision:** SC13-4 uses `SC13_4_SKIP=true` guard (not `it.skip`) because harness is plain CJS not a test framework
- **Decision:** `13-CEREMONY-PROBE-REPRODUCED.md` authored analytically (Electron binary unavailable in CLI); Plan 13-02 must validate before implementing fix

### 2026-05-12 — Operational

- **Disaster:** `gsd-planner` subagent destroyed `.planning/` (via `git clean -fdx` or similar) during Phase 13 planning. Recovery impossible (no shadow copies / File History / OneDrive sync; `.planning/` is gitignored; stashes were `-u`-less).
- **Mitigation:** Rebuilt REQUIREMENTS.md, ROADMAP.md, STATE.md, Phase 13 plans from git history + Claude conversation context. Phase 01-12 SUMMARY/PLAN/VERIFICATION artifacts are gone; the *code* they produced is unaffected.
- **Process change:** Going forward, do NOT spawn `gsd-planner` with broad Bash latitude. Either (a) tell it explicitly "no destructive git or filesystem commands — Write tool only for plan files, Read/Grep/Glob for inspection," or (b) author plans directly without spawning a subagent for high-stakes phases. For this session, Phase 13 plans were authored directly without a planner subagent.

---

## History

### Closed Milestones

| Milestone | Closed | Phases |
|-----------|--------|--------|
| v1.0      | (date lost — see git log first commits 2026-04) | 01, 02 |
| v1.1      | (date lost) | 03, 04, 05, 06, 07 |
| v1.2      | Closed (2026-05-13) | 08, 09, 10, 11, 12, 13 |

### Closed Phases (Phase NN — short description, close date approximated from latest commit)

| Phase | Title | Closed (latest commit date) |
|-------|-------|------------------------------|
| 01    | Fork Bringup | ~2026-04-29 |
| 01.1  | Build Toolchain & Fork-Hygiene Gap Closure | ~2026-05-05 |
| 01.2  | LFS Push-Ability Gap Closure | ~2026-05-05 |
| 02    | Bitemporal Graph Substrate | ~2026-04-29 |
| 03    | Edge Traversal + Retrieval + Reasoning Receipt | ~2026-04-30 |
| 04    | Verification Canvas (per-save, tiered) | ~2026-05-06 |
| 05    | Telemetry Harvester + Portability Filter | ~2026-05-06 |
| 06    | MCP Gateway (Consume 4, Expose 1) | ~2026-05-07 |
| 07    | Drift Detection + Contract Locking | ~2026-05-07 |
| 08    | Bridge Runtime Path Fixes (Durable) | ~2026-05-10 |
| 09    | Build & Launch Ergonomics | ~2026-05-10 |
| 10    | Bridge Production Polish | ~2026-05-10 |
| 11    | Visual Ceremony | ~2026-05-11 |
| 12    | Robustness Hardening | 2026-05-12 |
| 13    | v1.2 Closeout (pre-requisite) — CLOSE-01/02/03 | 2026-05-13 |
| 14    | Foundation RPCs — DEEP-01 rationale-chain RPC + DEEP-04 historical-conflict + DEEP-05 session-priority lens | 2026-05-14 |
| 15    | Graph Inspector Panel — DEEP-02 Cytoscape time-travel inspector + QueryGraphSnapshot + QueryTimelineTransitions RPCs | 2026-05-15 |
| 16    | Ripple Analysis + Cross-Repo Schema Migration — DEEP-03 constraint-lift hypothetical-impact + DEEP-06-A repo_id migration | 2026-05-15 |
