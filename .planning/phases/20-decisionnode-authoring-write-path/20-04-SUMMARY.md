---
phase: 20-decisionnode-authoring-write-path
plan: 04
subsystem: bridge-save-gate
tags: [auth-02, dispatchHover, recordRejection, mandate-d-fence, hover-tier, post-hoc-reject]

# Dependency graph
requires:
  - phase: 17
    provides: dispatchHover function (POLISH-04 — compact hover dispatch for benign-tier saves)
  - phase: 20-01
    provides: Mandate B BANNED-array extension (createDecisionNode) + Mandate A canvas/*.ts fence widening (commits cdea35d6667, 454080f2eb8)
  - phase: 04
    provides: kernel.recordRejection RPC + signature (RecordRejectionParams: receipt_id, change_id, note)
provides:
  - dispatchHover gains 'Reject' action button beside 'Open full receipt' (post-hoc reject path)
  - showWarningMessage modal confirm + kernel.recordRejection call wired in dispatchHover Step 4
  - note literal 'user_post_hoc_reject_benign_hover' distinguishes post-hoc reject from inline-tier Dismiss path
  - try/catch on recordRejection so RPC failure surfaces but doesn't crash save-gate hot path
  - Mandate D fence comment block (lines 499-508) preserved byte-identical to HEAD
  - Pitfall F caller-count fence preserved (dispatchHover identifier count == 2)
affects:
  - 20-05 (phase verify + closure)
  - future v2.2 work that may consume the post-hoc reject signal in the graph (OpenQuestion + references edge)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Post-hoc reject button in VS Code save-gate: showInformationMessage with multiple actions + nested showWarningMessage modal {modal:true} for destructive-decision confirm"
    - "try/catch around kernel RPC inside user-interaction handler to surface failures via console.error without crashing the host extension"
    - "Early-return after Reject branch to skip Open-full-receipt fallthrough — user has already decided"

key-files:
  created: []
  modified:
    - src/vs/goatide/extensions/goatide-bridge/src/save-gate/tier-dispatch.ts (dispatchHover Step 4: +30 -6 lines)
    - extensions/goatide-bridge/dist/save-gate/tier-dispatch.js (mirror, rebuilt via prepare_goatide.sh)

key-decisions:
  - "Reject branch placed BEFORE Open-full-receipt branch with early return — clear precedence; user clicking Reject is a decision, not a navigation prefix"
  - "Note literal 'user_post_hoc_reject_benign_hover' (verbatim per OQ#1+OQ#2 resolution) — distinguishes this post-hoc reject from the inline-tier Dismiss path's `decision.note`"
  - "try/catch wraps recordRejection only (not the showWarningMessage call) — modal cancellation is normal flow, RPC failure is the only error path worth logging"
  - "Bridge mirror path is `dist/save-gate/` not `out/save-gate/` (plan's `out/` reference is an artifact-path mismatch — actual bridge uses `dist/` per main field in package.json and Phase 14-19 convention)"

patterns-established:
  - "Pattern: showInformationMessage with N actions + nested showWarningMessage{modal:true} for destructive-decision confirm is the v2.1 paradigm for post-hoc save-gate decisions"
  - "Pattern: kernel RPC calls inside user-interaction handlers MUST be wrapped in try/catch with console.error('[goatide-bridge] <op> failed', e); silent crashes degrade trust in the save-gate"

requirements-completed: [AUTH-02]

# Metrics
duration: 13min
completed: 2026-05-17
---

# Phase 20 Plan 04: Reject Button Post-Hoc Wiring Summary

**dispatchHover Step 4 gains 'Reject' action button + kernel.recordRejection RPC wiring with note literal `user_post_hoc_reject_benign_hover`, closing AUTH-02 user-facing surface for benign-tier saves.**

## Performance

- **Duration:** 13 min
- **Started:** 2026-05-18T01:45:04Z
- **Completed:** 2026-05-18T01:58:10Z (approx)
- **Tasks:** 1 (Task 20-04-01)
- **Files modified:** 1 src + 1 mirror artifact (regenerated, not committed — mirror is dist/ which is build output)

## Accomplishments

- AUTH-02 user-facing surface landed: benign-tier hover saves now offer a post-hoc Reject button alongside the existing 'Open full receipt' button
- Reject path wired correctly: info notification action 'Reject' → showWarningMessage modal confirm 'Reject' → `kernel.recordRejection({receipt_id, change_id, note: 'user_post_hoc_reject_benign_hover'})`
- Mandate D structural fence preserved: dispatchHover still only reachable on `(tier='silent' AND isDestructive=false AND benignSetting='hover')`
- Pitfall F caller-count fence preserved: `grep -c "\bdispatchHover\b" tier-dispatch.ts` == 2
- Mandate D fence comment block (lines 499-508) byte-identical to HEAD
- Phase 19 SC3b regression gate: **13/13 SCs PASS, EXIT 0**

## Task Commits

1. **Task 20-04-01: Modify dispatchHover Step 4 + sync bridge mirror** — `61bb7a1973a` (feat)

_Note: Bridge mirror sync (`extensions/goatide-bridge/dist/`) is build output regenerated by `prepare_goatide.sh` — not committed because `dist/` is gitignored or treated as build artifact (no diff lines reported by `git status --short` for the mirror after sync)._

## Files Created/Modified

- `src/vs/goatide/extensions/goatide-bridge/src/save-gate/tier-dispatch.ts` — dispatchHover Step 4 gains Reject branch; +30 -6 lines (1 file, 36 total diff lines)
- `extensions/goatide-bridge/dist/save-gate/tier-dispatch.js` — mirror artifact rebuilt by `npm run build` + `prepare_goatide.sh`; contains note literal `user_post_hoc_reject_benign_hover` (2 occurrences = 1 in compiled code + 1 in JSDoc comment)

## Decisions Made

- **Reject precedence:** Reject branch placed BEFORE Open-full-receipt with explicit early `return`. Rationale: user clicking Reject is a decision; not letting that fall through to Open-full-receipt prevents accidental modal-on-modal stacking.
- **Note literal verbatim from plan:** `user_post_hoc_reject_benign_hover` (per OQ#1+OQ#2). This distinguishes the post-hoc benign reject from the inline-tier Dismiss path which uses `decision.note` (free-form user text).
- **try/catch scope:** Only `recordRejection` is wrapped. The `showWarningMessage` modal returning `undefined` (user dismissed) is normal flow and intentionally fails through to the `return` at the end of the Reject branch.
- **Mirror path mismatch (artifact, not bug):** Plan 20-04 text references `extensions/goatide-bridge/out/save-gate/tier-dispatch.js` but the actual repo layout uses `extensions/goatide-bridge/dist/save-gate/tier-dispatch.js` (per bridge's `package.json` `main: "./dist/extension.js"`). All Phase 14-19 work used `dist/`. Mirror byte-equality gate `refuse-stale-bridge-mirror.sh` exit 0 confirms the actual path is canonical.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Bridge mirror node_modules missing (Cannot find module 'ulid')**
- **Found during:** Task 20-04-01 Step 13 (Phase 19 SC3b smoke regression check)
- **Issue:** Initial smoke run scored 10/13 with SC3b/SC9/SC11 all SOFT-FAIL. Root cause from `exthost.log`: `Error: Cannot find module 'ulid'` when activating `goatide.goatide-bridge` — bridge mirror at `extensions/goatide-bridge/node_modules/` was absent entirely. `prepare_goatide.sh` had emitted `WARN: bridge mirror npm ci failed; built-in load will fail at activate-time` because Node v22.22.3 environment vs. (some) version mismatch in package-lock.
- **Fix:** Applied documented fallback from STATE.md 2026-05-17 decision (Plan 18-02): `cd extensions/goatide-bridge && npm install --omit=dev --ignore-scripts`. Restored full bridge dependencies including `ulid`.
- **Files modified:** `extensions/goatide-bridge/node_modules/` (untracked / build output, not committed) + `extensions/goatide-bridge/package-lock.json` (untracked diff from prepare_goatide.sh mirror sync — pre-existing, not introduced by this plan)
- **Verification:** Re-ran `node scripts/test/phase18-smoke-cdp.cjs` → **13/13 SCs PASS, EXIT 0** (Phase 19 SC3b gate fully GREEN).
- **Committed in:** N/A (environmental fix, no source changes — node_modules is build artifact, package-lock.json diff is pre-existing working-tree state unrelated to Plan 20-04 scope)

**Total deviations:** 1 auto-fixed (1 blocking environmental gap).

**Impact on plan:** Necessary to verify Plan 20-04's success criterion (Phase 19 SC3b 13/13 PASS regression gate). No source code added; restored documented environmental state. Pure infrastructure recovery, no scope creep.

## Issues Encountered

### Plan 20-01 RED stubs do NOT exist in working tree

- **Observation:** Plan 20-04 verification expects `dispatchHover-reject-button.test.ts` and `dispatchHover-reject-confirm.test.ts` to exist as Wave-0 RED stubs (authored by Plan 20-01). Neither file is present at `src/vs/goatide/extensions/goatide-bridge/test/unit/save-gate/`. Only `mandate-d-destructive-no-hover.test.ts` (Phase 17 artifact) and `save-gate-resource-scope.test.ts` (Phase 17 artifact) exist.
- **Workflow context:** Phase 20 git history shows 3 commits prior to Plan 20-04: `454080f2eb8` (Mandate B BANNED-array extension), `cdea35d6667` (Mandate A canvas/*.ts fence widening), `6768e7985d5` (Plan 20-02 kernel RPC + handler). Plan 20-01's RED-stub authoring tasks (2 dispatchHover stubs + Mandate D matrix `recordRejectionCalls` column extension) were NOT executed.
- **Impact on Plan 20-04 success criteria:**
  - The 2 RED→GREEN flips described in plan success criteria CANNOT be verified (stubs don't exist).
  - The Mandate D matrix `recordRejectionCalls === 0` invariant CANNOT be verified (the column extension is part of Plan 20-01's unfinished work). The matrix test as it exists at HEAD passes 3/3 with the structural invariants (4×3 snapshot, caller-count fence, hover-only invocation) intact.
- **Resolution:** Plan 20-04 code change itself is self-contained and correct. The post-hoc Reject branch lands in the bridge binary. Plan 20-01's missing RED-stubs are out-of-scope for Plan 20-04's commit boundary (the plan author's tasks block defines ONE task touching ONE file). Logged here for Plan 20-05 (phase verify + closure) to address before phase closure.

### Plan 20-02 bridge-side mirror uncommitted in working tree

- **Observation:** `src/vs/goatide/extensions/goatide-bridge/src/kernel/client.ts` and `src/vs/goatide/extensions/goatide-bridge/src/kernel/methods.ts` have uncommitted edits adding `CreateDecisionNodeRequest` + `KernelClient.createDecisionNode` method. The kernel-side Plan 20-02 work IS committed (`6768e7985d5`).
- **Impact on Plan 20-04:** None on commit scope. These uncommitted changes pre-existed in working tree before this session and are independent of `tier-dispatch.ts`. They were included in the `npm run build` output (the mirror bundle now contains the createDecisionNode bridge method) but no source files outside Plan 20-04 scope were modified during this execution.
- **Resolution:** Plan 20-04 commit only includes `tier-dispatch.ts` (verified via `git diff --cached --stat` showing 1 file, 36 lines). The uncommitted Plan 20-02 bridge-side work remains in working tree for the user / Plan 20-03 execution to commit.

### Pre-existing FORK-04 gate failure (out of scope)

- **Observation:** `scripts/ci/refuse-vs-workbench-edits.sh` reports `src/vs/workbench/services/extensions/electron-browser/localProcessExtensionHost.ts` as a non-allowlisted upstream edit. Last touched by commit `049bdcf2868` (Plan 10-04, well predates Phase 20).
- **Impact on Plan 20-04:** None — pre-existing, file unchanged in working tree, already logged in `.planning/phases/20-decisionnode-authoring-write-path/deferred-items.md` by Plan 20-01.
- **Resolution:** Already-deferred item; no action.

### Pre-existing bridge test suite environmental flakiness (out of scope)

- **Observation:** `npm test` in bridge: 125 passing / 16 failing / 3 pending. ALL 16 failures are in `canvas/` React component tests (HypotheticalImpact, DriftFindings, CitationList, ConfirmationPhrase) or Phase 7 drift-flow integration or walkthrough-completion. Root cause appears to be jsdom `HTMLCanvasElement.prototype.getContext` not implemented (cytoscape renderer test infra). None touch save-gate or dispatchHover.
- **Impact on Plan 20-04:** None. POLISH-04 Mandate D suite (3/3 PASS), constraint.lift no-mutation suite GREEN, save-gate getConfiguration resource-scope GREEN.
- **Resolution:** Out of scope per execute-plan.md SCOPE BOUNDARY rule. Pre-existing environmental gap. Recorded here for visibility.

## Verification Evidence

| Gate | Result | Notes |
|---|---|---|
| Bridge tsc `npx tsc -p . --noEmit` | PASS | Zero errors (silent stdout) |
| Bridge `npm run build` (tsc + esbuild) | PASS | dist/canvas/index.js 32.3mb; dist/inspector/index.js 8.1mb; both rebuilt in 175ms + 575ms |
| Project tsc native `npm run compile-check-ts-native` | PASS | Zero errors (silent stdout) |
| `refuse-stale-bridge-mirror.sh` | PASS | Exit 0 — mirror byte-equal after `prepare_goatide.sh` |
| Note literal `user_post_hoc_reject_benign_hover` in `extensions/goatide-bridge/dist/save-gate/tier-dispatch.js` | PASS | 2 occurrences (1 code + 1 JSDoc) |
| Pitfall F caller-count fence | PASS | `grep -c "\bdispatchHover\b" tier-dispatch.ts` == 2 (line 322 caller + line 509 declaration) |
| Mandate D fence comment block (lines 499-508) | PASS | byte-identical to HEAD (preserved verbatim via Edit's precise old_string anchor) |
| POLISH-04 Mandate D matrix test | PASS | 3/3 (4x3 snapshot + hover-only invariant + caller-count fence) |
| 5 `refuse-*.sh` CI gates (production-relevant) | PASS | refuse-deep05-write, refuse-stale-bridge-mirror, refuse-llm-in-canvas (via meta), refuse-credential-leaks-in-fixtures, refuse-silent-override — all exit 0. (refuse-vs-workbench-edits FAIL is pre-existing FORK-04 issue out of scope) |
| 4 meta-tests | PASS | refuse-deep05-write.meta, refuse-llm-in-canvas.meta, refuse-stale-bridge-mirror-after-walkthrough.meta, refuse-stale-bridge-mirror-after-configurationDefaults.meta — all `META PASS` |
| Phase 19 SC3b smoke regression | PASS | `node scripts/test/phase18-smoke-cdp.cjs` → 13/13 SCs PASS, EXIT 0, 0 CDN hits |
| Plan 20-04 commit scope | PASS | Only `src/vs/goatide/extensions/goatide-bridge/src/save-gate/tier-dispatch.ts` modified (1 file, +30 -6) per `git diff --cached --stat` |

## User Setup Required

None - no external service configuration required.

## Next Plan Readiness

- **Plan 20-05 (phase verify + closure)** can proceed when:
  1. Plan 20-01's missing RED stubs (`dispatchHover-reject-button.test.ts` + `dispatchHover-reject-confirm.test.ts`) are authored and confirmed GREEN against this commit's `tier-dispatch.ts`, OR Plan 20-05 documents the structural verification path used in lieu of those stubs.
  2. Plan 20-02 bridge-side mirror uncommitted changes (kernel/client.ts, kernel/methods.ts) are committed (Plan 20-03 dependency).
  3. Plan 20-03 (canvas/authoring-flow.ts + extension.ts command swap) executed.
- **Phase-level invariants intact:** Mandate D structural fence GREEN, Mandate B fence (refuse-deep05-write.sh) GREEN, Mandate A canvas fence (refuse-llm-in-canvas.meta.sh) GREEN, Phase 19 SC3b regression gate 13/13 GREEN.

## Self-Check: PASSED

Verification of claims in this SUMMARY:

- `src/vs/goatide/extensions/goatide-bridge/src/save-gate/tier-dispatch.ts` — FOUND (modified, committed in 61bb7a1973a)
- `extensions/goatide-bridge/dist/save-gate/tier-dispatch.js` — FOUND (mirror artifact, contains note literal)
- Commit hash `61bb7a1973a` — FOUND in `git log --oneline -5` as `feat(20-04): AUTH-02 wave-2 dispatchHover Reject button + recordRejection wiring`
- Smoke harness exit 0 — confirmed via direct invocation, 13/13 SCs PASS

---
*Phase: 20-decisionnode-authoring-write-path*
*Plan: 04*
*Completed: 2026-05-17*
